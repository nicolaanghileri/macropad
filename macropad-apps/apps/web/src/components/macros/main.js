import BigButton from './../buttons/BigButton';
import SmallButton from './../buttons/SmallButton';

const MainMacro = (props) => {
    const content = props.content || 'Default';
    const icon = props.icon || null;

    return (
        <>
        
        </>
    );

}



export default MainMacro;